<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.finalproject">
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.AdminLoginActivity"/>
        <activity android:name=".view.UserLoginActivity"/>
<activity android:name=".view.VotingType"/>
        <activity android:name=".view.AdminPage"/>
        <activity android:name=".view.UserRegister"/>
        <activity android:name=".view.UserFingerPrintAuth"/>
        <activity android:name=".view.UploadVoting"/>
    </application>

</manifest>